global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/license.rules.yml

scrape_configs:
  - job_name: licenses_exporter
    metrics_path: /metrics
    static_configs:
      - targets: ["licenses_exporter:9105"]
